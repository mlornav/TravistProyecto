language: java
jdk:
  - openjdk11

# Comando para instalar las dependencias y compilar el proyecto
install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

# Comando para ejecutar las pruebas con JUnit
script: mvn test
